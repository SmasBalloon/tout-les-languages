import React from 'react';

const App: React.FC = () => {
    return (
        <div>
            <h1>Bonjour, monde!</h1>
            <p>Ceci est un composant React de base.</p>
        </div>
    );
};

export default App;